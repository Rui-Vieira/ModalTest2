/*
import {
    PopUpContainer,
    PopUpFormContainer,
    FormTitle,
    SendInviteBtn,
    CloseFormBtn
} from "./popUpStyles";

interface ISetModalOpen {
    setModalIsOpen: (value: boolean) => void;
}

export const InvitationSentForm = ({ setModalIsOpen }: ISetModalOpen) => {


    return (
        <PopUpContainer>
            <CloseFormBtn onClick={() => setModalIsOpen(false)}>x</CloseFormBtn>

            <PopUpFormContainer>
                <FormTitle className="form-title">

                    <p>
                        Your invitation has been successfully sent
                    </p>
                </FormTitle>

                <SendInviteBtn onClick={() => setModalIsOpen(false)} >
                    Close
                </SendInviteBtn>
            </PopUpFormContainer>

        </PopUpContainer>

    );
};

export default InvitationSentForm;

*/